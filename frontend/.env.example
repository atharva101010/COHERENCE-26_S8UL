# In production, leave VITE_API_URL empty so requests go to the same origin
# VITE_API_URL=https://your-backend-url.onrender.com

# For local dev, this is not needed (defaults to http://localhost:3001)
